# Build-time Environment Variables
# Usato durante `npm run build` su host (non Docker)

NODE_ENV=production
NEXT_PUBLIC_APP_URL=https://oz.fodivps2.cloud

# Database (localhost per build-time, vps-panel-postgres per runtime)
DATABASE_URL="postgresql://oz_user:OZ_SECURE_PASSWORD_123@localhost:5432/oz_db"
